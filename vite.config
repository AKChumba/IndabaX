import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  // This MUST match the folder path where the site is actually served from
  // on cPanel: https://indabaxnamibia.com/2026X/
  base: '/',

  plugins: [react()],

  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  },
})