# IndabaX Namibia Website

For the brain to pulse you need to run the terminal, run npm run dev, thus opening localhost.

## Quick start

1. Install dependencies
   - Run: `npm install`
2. Start the development server
   - Run: `npm run dev`
3. Build for production
   - Run: `npm run build`
